# Визуальный гайд InnerQuest: Город Сфер

## Концепция визуального стиля

**Ключевая фраза**: "Ламповое уютное фэнтези"

**Настроение**: 
- Вечер в уютной кофейне с видом на мистический город
- Тёплый свет свечей и фонарей
- Мягкий туман, который не пугает, а создаёт атмосферу
- Рукописная магия (hand-painted digital art)

**Анти-примеры** (чего избегать):
- ❌ Холодный киберпанк
- ❌ Резкие неоновые цвета
- ❌ Агрессивные тени
- ❌ Стерильный минимализм

**Про-примеры** (к чему стремимся):
- ✅ Gris (игра) — мягкая акварель
- ✅ Journey — минимализм с теплом
- ✅ Spiritfarer — ламповая магия
- ✅ Coffee Talk — уютная атмосфера

---

## Цветовая палитра

### Базовая палитра (нейтральная)

| Цвет | Hex | Использование |
|------|-----|---------------|
| **Глубокий индиго** | `#1a1f2e` | Основной фон |
| **Тёмный аспидный** | `#0f1419` | Тени, контейнеры |
| **Серо-синий** | `#2a3142` | Вторичный фон |
| **Мягкий белый** | `#f5f5f0` | Основной текст |
| **Приглушённый серый** | `#b0b8d0` | Вторичный текст |

### Акцентные цвета (тёплые)

| Цвет | Hex | Использование |
|------|-----|---------------|
| **Золотистый** | `#ffd89b` | Подсветки, награды |
| **Тёплый оранжевый** | `#ff9a56` | Кнопки, акценты |
| **Мягкий персиковый** | `#ffb396` | Hover-эффекты |
| **Мятный** | `#a8e6cf` | Успех, здоровье |
| **Лавандовый** | `#c7b3e5` | Мистика, магия |

### Цвета по кварталам

| Квартал | Основной | Акцент | Свечение | Настроение |
|---------|----------|--------|----------|------------|
| **🌿 Оазис** | `#4d8b6f` | `#a8e6cf` | `#c5f5dc` | Зелёно-мятная свежесть |
| **🏛️ Цитадель** | `#4a7ba7` | `#6bb6ff` | `#a8d5ff` | Холодный синий + золото |
| **⚔️ Арсенал** | `#7b5ba1` | `#b088d9` | `#d4c5f0` | Фиолетово-пурпурная тайна |
| **🤝 Форум** | `#d97942` | `#ff9a56` | `#ffc299` | Тёплый оранжевый огонь |
| **🌱 Сад** | `#b36f8c` | `#e5a4c3` | `#f5d5e5` | Розовато-лавандовая нежность |

### Градиенты

**Для кнопок**:
```css
background: linear-gradient(135deg, #ff9a56 0%, #d97942 100%);
```

**Для карт (по редкости)**:
- **Common**: `linear-gradient(135deg, #8b7355 0%, #6b5644 100%)` (бронза)
- **Rare**: `linear-gradient(135deg, #9ca3af 0%, #6b7280 100%)` (серебро)
- **Epic**: `linear-gradient(135deg, #ffd89b 0%, #ff9a56 100%)` (золото)
- **Legendary**: `linear-gradient(135deg, #c7b3e5 0%, #9b7ec0 100%)` (фиолетовый)

**Для фона Айры**:
```css
background: radial-gradient(circle at 30% 40%, 
    rgba(255, 216, 155, 0.15) 0%, 
    rgba(26, 31, 46, 0.8) 60%, 
    #1a1f2e 100%
);
```

---

## Типографика

### Шрифты

**Основной текст**:
- Семейство: `'Inter', 'SF Pro Display', system-ui, sans-serif`
- Размер: `16px` (базовый)
- Вес: 400 (обычный), 600 (заголовки)
- Интерлиньяж: `1.6` (для читаемости)

**Заголовки**:
- Семейство: `'Playfair Display', 'Georgia', serif` (для названия игры)
- ИЛИ `'Cinzel', serif` (для сюжетных текстов)
- Размер: `2.5rem` (H1), `1.8rem` (H2)
- Вес: 300-400 (лёгкий, элегантный)

**Карточки**:
- Названия: `'Cinzel', serif` (14px, 600)
- Описания: `'Inter', sans-serif` (12px, 400)

**Айра**:
- Реплики: `'Inter', sans-serif` (16px, 400)
- Имя: `'Cinzel', serif` (24px, 600)

### Буквица и акценты

Для сюжетных текстов (Lore-карты, диалоги боссов):
- Первая буква увеличена (`1.8em`)
- Тень для глубины (`text-shadow: 2px 2px 4px rgba(0,0,0,0.3)`)

---

## Анимации и эффекты

### Принципы анимации

1. **Плавность**: easing `cubic-bezier(0.4, 0.0, 0.2, 1)`
2. **Медленно**: длительность 0.3-0.5s (не быстро!)
3. **Тонкость**: не более 10% изменения (subtle movement)
4. **Дыхание**: pulsing эффекты (не мигание)

### Ключевые анимации

**Появление карты**:
```css
@keyframes cardAppear {
    0% {
        opacity: 0;
        transform: translateY(30px) scale(0.8);
        filter: blur(10px);
    }
    100% {
        opacity: 1;
        transform: translateY(0) scale(1);
        filter: blur(0);
    }
}
animation: cardAppear 0.8s cubic-bezier(0.4, 0.0, 0.2, 1);
```

**Туман (fog)**:
```css
@keyframes fogDrift {
    0%, 100% { 
        transform: translateX(0) translateY(0); 
        opacity: 0.6; 
    }
    50% { 
        transform: translateX(20px) translateY(-10px); 
        opacity: 0.4; 
    }
}
animation: fogDrift 20s ease-in-out infinite;
```

**Дыхание Айры**:
```css
@keyframes breathe {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.02); }
}
animation: breathe 4s ease-in-out infinite;
```

**Мерцание огней в окнах**:
```css
@keyframes lightFlicker {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
}
animation: lightFlicker 3s ease-in-out infinite;
```

### Hover-эффекты

**Кварталы города**:
```css
.district:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 40px var(--district-color-alpha-30);
    filter: brightness(1.2);
}
transition: all 0.4s cubic-bezier(0.4, 0.0, 0.2, 1);
```

**Карточки**:
```css
.card:hover {
    transform: translateY(-8px) rotate(1deg);
    box-shadow: 0 15px 50px rgba(255, 216, 155, 0.4);
    z-index: 100;
}
transition: all 0.3s ease;
```

---

## Композиция экранов

### Загрузочный экран

**Композиция**:
- Центр: Название игры (крупно)
- Средний план: Силуэт города (из тумана)
- Передний план: Анимация тумана (дрейфует)
- Низ: Прогресс-бар (тонкий, золотистый)

**Соотношение**:
- 20% — небо/верх
- 50% — город
- 30% — туман/низ

**Освещение**: Снизу вверх (как рассвет)

---

### Карта города

**Композиция**:
- Сетка 2x3 или 3x2 (в зависимости от ширины экрана)
- Центральный квартал крупнее (текущий фокус)
- Туман сильнее на заблокированных
- Огни в окнах на открытых

**Глубина**:
- Задний план: Силуэт остального города
- Средний план: 5 кварталов (интерактивные)
- Передний план: Статистика (полупрозрачная панель)

---

### Экран диалога с Айрой

**Композиция** (две колонки):

**Левая (30%)**:
- Верх: Портрет Айры (большой, 300-400px)
- Середина: Карточки с правилами
- Низ: Концепция города

**Правая (70%)**:
- Верх: Аватар Айры (маленький, для контекста)
- Центр: Окно чата (скроллится)
- Низ: Поле ввода + кнопка

**Вертикальный ритм**:
- Портрет: 400px
- Карточки: по 150-200px
- Чат: flex-grow (остальное пространство)

---

## Визуальные метафоры

### Туман = Неопределённость

**Визуализация**:
- Не плотный (видно сквозь него)
- Тёплый оттенок (не холодный серый)
- Частицы дрейфуют медленно
- При прогрессе: рассеивается от центра к краям

**CSS**:
```css
.fog {
    background: radial-gradient(circle, 
        rgba(180, 180, 200, 0.4) 0%, 
        rgba(100, 100, 120, 0.2) 50%, 
        transparent 100%
    );
    backdrop-filter: blur(8px);
}
```

---

### Огни в окнах = Прогресс

**Визуализация**:
- Маленькие светящиеся точки
- Цвет: золотистый (`#ffd89b`)
- Мерцают асинхронно
- Количество = уровень квартала × 2

**CSS**:
```css
.window-light {
    width: 4px;
    height: 4px;
    background: #ffd89b;
    border-radius: 50%;
    box-shadow: 0 0 8px #ffd89b, 0 0 12px rgba(255, 216, 155, 0.6);
    animation: lightFlicker var(--random-duration) ease-in-out infinite;
}
```

---

### Карточки = Артефакты

**Визуализация**:
- Как старинные карты Таро, но цифровые
- Рамка светится по редкости
- Иконка в центре (hand-drawn стиль)
- Текст читаемый, но элегантный

**Размеры**:
- Ширина: 180px
- Высота: 280px (соотношение ~2:3)
- Радиус скругления: 12px
- Бордер: 2px, светящийся

**Структура карты**:
```
┌─────────────────┐
│  [Иконка 80px]  │ ← В верхней трети
│                 │
│  Название       │ ← Центр
│  ───────        │
│  Эффект (текст) │ ← Описание
│                 │
│  [Редкость]     │ ← Низ
└─────────────────┘
```

---

## Освещение и тени

### Принципы

1. **Источник света**: Сверху-слева (как окно)
2. **Тёплый тон**: Золотистый (`#ffd89b`), не белый
3. **Мягкие тени**: `box-shadow` с большим размытием
4. **Внутреннее свечение**: для магических элементов

### Примеры теней

**Карточки**:
```css
box-shadow: 
    0 10px 30px rgba(0, 0, 0, 0.5),
    0 0 20px rgba(255, 216, 155, 0.3),
    inset 0 1px 1px rgba(255, 255, 255, 0.1);
```

**Кварталы**:
```css
box-shadow: 
    0 5px 20px rgba(0, 0, 0, 0.4),
    0 0 40px var(--district-glow-color);
```

**Айра (аватар)**:
```css
box-shadow:
    0 0 40px rgba(255, 216, 155, 0.6),
    0 0 80px rgba(255, 216, 155, 0.3),
    inset 0 0 20px rgba(255, 255, 255, 0.1);
```

---

## Композиционные правила

### Правило третей

Важные элементы располагаются на пересечении линий третей:
- Портрет Айры: верхняя треть
- Кнопка действия: нижняя треть
- Фокусный элемент: центр

### Воздух и дыхание

**Пространство вокруг элементов**:
- Минимум `20px` padding вокруг контента
- `40px` между крупными блоками
- `60px` от краёв экрана до контента

**Не переполнять**: лучше меньше элементов, но крупнее и читаемее

---

## Иконография

### Стиль иконок

**Кварталы**:
- Эмодзи (для быстроты) ИЛИ custom SVG
- Размер: 64-80px
- Мягкий outline (не solid fill)
- Тонкая линия: 2px

**Карточки**:
- Hand-drawn стиль
- Линии неровные (человечные)
- Заливка: gradient или texture
- Акцент на узнаваемости

### Референсы для иконок

- **Skill**: Руки, инструменты, действие
- **Relic**: Артефакты, древние предметы
- **Contract**: Свитки, печати, соглашения
- **Lore**: Книги, свитки, руны
- **Companion**: Портреты персонажей
- **Hazard**: Абстрактные формы (шум, туман, цепи)

---

## Эффекты частиц

### Туман (fog particles)

```javascript
// Параметры
count: 20-30 частиц
size: 40-80px (random)
opacity: 0.1-0.3
speed: 10-20s для полного цикла
direction: drift (не прямо)
```

### Искры (при получении очков)

```javascript
count: 15-20
size: 3-6px
color: #ffd89b
lifetime: 1.5s
pattern: взрыв из центра, затухание
```

### Свечение (glow around active elements)

```css
filter: drop-shadow(0 0 10px var(--glow-color));
```

---

## Адаптивность

### Breakpoints

- **Desktop**: >1200px — полный лейаут, 2 колонки
- **Tablet**: 768-1199px — одна колонка, панель Айры сворачивается
- **Mobile**: <768px — минималистичный, вертикальная прокрутка

### Mobile-first правила

1. Текст крупнее: базовый 18px (не 16px)
2. Кнопки больше: min-height 48px (для пальца)
3. Карточки в ряд: 1 карта на экран, свайп
4. Айра: только аватар, полный портрет в отдельном окне

---

## Анимированные переходы между экранами

### Fade + Slide

```css
.screen-enter {
    animation: screenEnter 0.6s cubic-bezier(0.4, 0.0, 0.2, 1);
}

@keyframes screenEnter {
    0% {
        opacity: 0;
        transform: translateY(40px);
    }
    100% {
        opacity: 1;
        transform: translateY(0);
    }
}
```

### Cross-fade (для спокойных переходов)

```css
.screen-exit {
    animation: screenExit 0.4s ease-out;
}

@keyframes screenExit {
    0% { opacity: 1; }
    100% { opacity: 0; }
}
```

---

## Микроанимации (delight)

### Кнопки при клике

```css
.btn:active {
    transform: scale(0.95);
}
```

### Карточка при получении

```css
@keyframes cardReceived {
    0% {
        transform: scale(0) rotate(-10deg);
        opacity: 0;
    }
    60% {
        transform: scale(1.1) rotate(5deg);
    }
    100% {
        transform: scale(1) rotate(0);
        opacity: 1;
    }
}
animation: cardReceived 0.8s cubic-bezier(0.68, -0.55, 0.265, 1.55);
```

### Очки устойчивости (counter)

```css
@keyframes countUp {
    0% { transform: scale(1); }
    50% { transform: scale(1.3); color: #4CAF50; }
    100% { transform: scale(1); }
}
```

---

## Accessibility (доступность)

### Цветовая контрастность

Все тексты должны иметь контраст минимум **4.5:1**:
- Белый на тёмном: ✅ 12:1
- Золотистый на тёмном: ✅ 7:1
- Серый вторичный: ✅ 4.8:1

### Альтернативы для цвета

Редкости карт различаются не только цветом, но и:
- Паттерном рамки (штрихи, точки)
- Количеством звёзд
- Иконкой редкости

### Увеличение текста

Все размеры в `rem`, чтобы масштабироваться с настройками браузера

---

## Звуковой дизайн (опционально)

### Ambient звуки

**Город**:
- Тихий city ambience (далёкие голоса, шаги)
- Ветер (subtle)
- Колокольчики (редко, для атмосферы)

**Кварталы** (при наведении):
- Оазис: вода, птицы
- Цитадель: перелистывание страниц
- Арсенал: тихий звон монет
- Форум: приглушённый говор
- Сад: ветер в листьях

### UI звуки

- Клик кнопки: мягкий "click" (не резкий)
- Открытие карты: магический "whoosh" + shimmer
- Получение очков: приятный "ding" (не громкий)
- Айра печатает: тихий набор на клавиатуре

**Громкость**: По умолчанию 30%, регулируется в настройках

---

## Референсы (для вдохновения)

### Игры
- **Gris** — акварельная эстетика
- **Journey** — минимализм с эмоцией
- **Spiritfarer** — тёплая ламповость
- **Coffee Talk** — уютная атмосфера
- **Slay the Spire** — дизайн карт
- **Hades** — UI с характером

### Художники
- **Atey Ghailan** — тёплые сцены, освещение
- **Loish** — мягкая цветопередача
- **Tyson Tan** — персонажи с теплом
- **Sam Does Arts** — уютное фэнтези

### Цветовые палитры
- **Warm Night** (coolors.co)
- **Cozy Autumn** (колористика осени)
- **Magical Study** (тёмный + золото)

---

## Чеклист визуального качества

Перед релизом проверить:

- [ ] Все тексты читаются (контраст >4.5:1)
- [ ] Анимации плавные (60fps)
- [ ] Нет резких переходов
- [ ] Hover-эффекты везде, где можно кликнуть
- [ ] Загрузка не блокирует UI (skeleton screens)
- [ ] Все картинки имеют fallback (если не загрузились)
- [ ] Туман не мешает читать текст
- [ ] Кнопки достаточно крупные (min 40x40px)
- [ ] Карточки помещаются на экран (не обрезаны)
- [ ] Айра выглядит тепло (не холодно/роботично)

---

Этот гайд — **визуальная философия** InnerQuest: тепло, мягкость, уют, магия без агрессии.

