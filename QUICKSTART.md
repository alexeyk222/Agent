# Быстрый старт InnerQuest

## 5 минут до первой сессии

### 1. Установка (1 минута)

```bash
# Распаковать архив
tar -xzf innerquest-game.tar.gz
cd innerquest

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка API (1 минута)

Откройте `config.py` и вставьте ваш OpenRouter API ключ:

```python
OPENAI_API_KEY = "sk-or-v1-ваш-ключ-здесь"
OPENAI_MODEL = "qwen/qwen-2.5-7b-instruct:free"  # Бесплатная модель
```

Получить ключ: https://openrouter.ai/keys

### 3. Запуск (30 секунд)

```bash
python app.py
```

Откройте браузер: http://localhost:5000

### 4. Первая сессия (5-10 минут)

1. Загрузится карта города
2. Выберите **Цитадель** (работа) или **Оазис** (здоровье)
3. Выберите эмоцию (например, "Выгорание")
4. Ответьте на 3-5 вопросов Айры
5. Выполните микрозадание (1-2 минуты реально!)
6. Получите награды и карты

---

## Что делать дальше

### После первой сессии:

- Откройте **Дневник** — посмотрите историю
- Задайте вопросы **Айре** (режим ГУРУ)
- Изучите открытые **карточки** справа от чата
- Вернитесь через 4 часа для новой сессии

### Полезные документы:

- `SCENARIOS.md` — как работают уровни кварталов
- `DESIGN_CARDS.md` — система карточек и правила
- `MODELS.md` — выбор AI-модели
- `VISUAL_GUIDE.md` — про визуальный стиль
- `IMAGE_PROMPTS.md` — если хотите генерировать изображения

---

## Частые вопросы

**Q: Нужно ли платить за API?**  
A: Нет, есть бесплатные модели. Смотрите `MODELS.md`.

**Q: Сколько времени занимает сессия?**  
A: 10-15 минут. Игра сама ограничивает время.

**Q: Данные хранятся локально?**  
A: Да, всё в `data/saves/player.json`.

**Q: Можно играть без интернета?**  
A: Нет, нужен доступ к OpenRouter API для диалогов с Айрой.

**Q: Где взять изображения для игры?**  
A: Смотрите `IMAGE_PROMPTS.md` и `IMAGE_LIST.md`. Можно играть без них (будут placeholders).

---

## Консольные команды (для разработчиков)

Откройте консоль браузера (F12):

```javascript
gameCommands.help()        // Список команд
gameCommands.cheat()       // Режим читов
gameCommands.debug()       // Отладочная информация
gameCommands.addPoints(50) // Добавить очки (только в cheat mode)
```

---

**Приятной игры и развития! 🌟**

Если возникли проблемы: проверьте `INSTALL.md` или консоль браузера.

