# Инструкция по установке и запуску

## Быстрый старт

1. **Распакуйте архив** `innerquest-game.tar.gz`

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте OpenAI API ключ:**
   - Откройте файл `config.py`
   - Замените `"sk-your-openai-api-key-here"` на ваш реальный ключ OpenAI
   - Ключ можно получить на https://platform.openai.com/api-keys

4. **Запустите игру:**
```bash
python app.py
```

5. **Откройте браузер:**
   - Перейдите по адресу: `http://localhost:5000`
   - Игра загрузится автоматически

## Важные замечания

- Убедитесь, что у вас установлен Python 3.8 или выше
- OpenAI API ключ должен быть действительным и иметь доступ к моделям GPT-4 или GPT-3.5-turbo
- Игра сохраняет прогресс в папке `data/saves/` в формате JSON

## Решение проблем

**Ошибка "Module not found":**
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`

**Ошибка "OpenAI API error":**
- Проверьте правильность API ключа в `config.py`
- Убедитесь, что у вас есть доступ к OpenAI API и есть баланс на счету

**Игра не запускается:**
- Проверьте, что порт 5000 свободен
- Попробуйте изменить порт в `config.py` (FLASK_PORT)

## Карточки и награды
- Карточки открываются детерминированно за Effort (микрошаги/сессии), без LLM
- Рейка: 5 слотов, 1 активная карта (Skill/Relic/Contract/Lore/Companion/Hazard)
- Сюжет и правила описаны в `DESIGN_CARDS.md`

## Первый запуск

При первом запуске игра создаст нового игрока с начальными настройками:
- 0 очков устойчивости
- Три разблокированных квартала: Цитадель, Арсенал, Сад
- Оазис и Форум заблокированы (Форум откроется при 50 очках)

Удачи в игре! 🌟

